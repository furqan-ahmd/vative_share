<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External files directory for your app -->
    <external-files-path name="external_files" path="." />
    
    <!-- External cache directory -->
    <external-cache-path name="external_cache" path="." />
    
    <!-- Internal cache directory -->
    <cache-path name="cache" path="." />
    
    <!-- Internal files directory -->
    <files-path name="files" path="." />
    
    <!-- External storage root -->
    <external-path name="external" path="." />
    
    <!-- Pictures directory specifically for image sharing -->
    <external-files-path name="pictures" path="Pictures" />
    
    <!-- Movies directory specifically for video sharing -->
    <external-files-path name="movies" path="Movies" />
</paths>